#include<stdio.h>
#include<math.h>

int main()
{
    int n,x,i,j,k,res=0,count=0;
    scanf("%d %d",&n,&x);
    int ara[1000];

    for(i=0;i<n;i++)
    {
        scanf("%d",&ara[i]);
    }


    for(i=0;i<n-1;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if()
             for(k=0;k<=m;k++)
           {
               d=pow(2,n);

               res+= d*(((j/d)%2 + (k/d)%2)%2);
           }
        }
    }
}
